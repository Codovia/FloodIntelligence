# FloodPulse Data Manifests

Large raw, intermediate, and generated datasets are intentionally excluded from
normal Git version control.

The repository stores:
- data acquisition/processing code;
- dataset provenance and metadata;
- reproducibility documentation.

Raw data must be obtained from the documented external sources in
`docs/DATA_SOURCES.md`.

Large datasets must never be committed directly to the normal Git history.
